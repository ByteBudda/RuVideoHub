name: Android Release Sign with Repo Key

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Build with Gradle (Release)
        run: |
          ./gradlew assembleRelease -Pfdroid=true \
            -Pandroid.injected.signing.store.file="../bytebudda.keystore" \
            -Pandroid.injected.signing.store.password="9z9r1h41n0r1" \
            -Pandroid.injected.signing.key.alias="bytebudda" \
            -Pandroid.injected.signing.key.password="9z9r1h41n0r1" \
            --no-daemon --no-configuration-cache
        working-directory: ./app

      - name: Upload APK to Release
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/release/app-release.apk
          tag_name: release-${{ github.run_number }}
          name: release Build #${{ github.run_number }}
          draft: true
          prerelease: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
